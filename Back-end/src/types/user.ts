export type userDTO = {
  user: string;
  email: string;
  password: string;
};

export type userLoginDTO = {
  user: string;
  password: string;
};
export type userDateDTO = {
  id: string;
  user: string;
  email: string;
  password: string;
};
export type loginDTO = {
  id: string;
  user: string;
  email: string;
  password: string;
};
